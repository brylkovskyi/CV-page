language: node_js
node_js:
  - "7"
script:
  - echo "Deploy!!"
install:
  - npm install -g firebase-tools
  - ng build --prod --aot
after_success:
  - firebase deploy --project cv-page-application --token $FIREBASE_TOKEN
